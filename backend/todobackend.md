TODO BACKEND

Paused for now — frontend (LeadDesk) is the active focus. Nothing here is being worked on until we come back to it.

- The AarthikLabs API contract (Dedupe, Lead Creation, Nearest Branch, Lead Status) matches the real spec doc almost exactly. Good.
- Real gap: the Lead Creation schema requires offer_accepted_at, but the sequence diagram shows expired/non-accepted leads also need to get pushed through the same flow. Right now there's no way to do that without violating the schema. Needs a decision from Ayushman/Nishit on how that's supposed to work.
- Unclear how Yellow Metal's LTV / gold-rate / serviceability rules actually reach AarthikLabs so they can compute offers on their side. Might be manual/out-of-band, might be a missing API. Worth asking rather than assuming.
- /api/internal/* still has zero auth. This was deferred until the real sample JSON arrived — it's arrived now, so this is overdue.
- Lead IDs generated by the backend (YMLEAD123456, random, unpadded) don't match the format AarthikLabs' own example shows (YMLEAD00128473, zero-padded 8 digits). Could fail validation on their end.
- updateLeadStatus lost its input validation in the recent restructure — used to reject bad status values, now accepts anything.
- getLeads (internal, dashboard-facing) lost pagination — returns the whole table every call now.
- There's a leaked API key sitting in the git history of the yellow_metal_ondc repo (an earlier commit, since fixed forward but not scrubbed from history). Should get rotated if it's a real key.
- Nearest Branch API always returns the same hardcoded Rajkot branch regardless of pincode — fine as a stub, not for real traffic.
- The webhook crash-recovery sweep (retryPendingWebhooks) was removed in the restructure — if the server restarts mid-retry, that webhook never resumes on its own anymore.
- offer.service.ts, lead.service.ts, and utils/officers.ts are now dead code after the restructure, not cleaned up.
- No rate limiting anywhere on the backend.
- The local index-fix commit (pincode index, composite webhook-retry index) is still only local, not pushed to GitHub, so Ayushman doesn't have it yet.
